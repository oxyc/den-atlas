# The build copies only Cargo.toml, Cargo.lock, src/ and data/; everything below is context it never reads.
target
.git
.github
.env
*.log
tests
docs
# NB: data/ is NOT ignored — the dataset blobs go into the image (empty in CI → mounted at runtime).
