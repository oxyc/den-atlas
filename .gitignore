node_modules/
dist/
coverage/
.env
*.log

# The dataset artifacts are large derived blobs, not source. They're imported from the Den repo (or a
# published mirror) via `npm run import` and baked into the Docker image at build — never committed.
data/*.json
data/*.bin
!data/.gitkeep
